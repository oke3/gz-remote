{
  "name": "opencode-remote",
  "version": "0.1.0",
  "description": "Zero-dependency CLI + library for driving opencode instances on remote machines over SSH.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "ocr": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"test/*.test.ts\"",
    "build": "tsc -p tsconfig.build.json",
    "smoke": "node dist/cli.js --help"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "author": {
    "name": "oke3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oke3/opencode-remote.git"
  },
  "homepage": "https://github.com/oke3/opencode-remote#readme",
  "bugs": {
    "url": "https://github.com/oke3/opencode-remote/issues"
  }
}
